[package]
name = "claimwright"
version = "0.1.0"
edition = "2021"
description = "Minimal policy substrate checker for ClaimWright"
license = "Apache-2.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
